<Project>

  <!--
    Durable Task on-demand sandbox preview package version.
    Override on the command line: dotnet build /p:DtsSdkVersion=<version>
  -->
  <PropertyGroup>
    <DtsSdkVersion Condition="'$(DtsSdkVersion)' == ''">1.25.0-preview.2</DtsSdkVersion>
  </PropertyGroup>

</Project>
